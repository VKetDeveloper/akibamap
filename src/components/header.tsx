'use client';
import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="bg-red-500 text-white p-4">
            <h1 className="text-2xl font-bold">VketReal AkibaMap(β)</h1>
        </header>
    );
};

export default Header;